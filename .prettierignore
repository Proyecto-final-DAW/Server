# Dependencies and build
node_modules
dist
build
coverage

# Lock files (don’t format)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Minified / generated
*.min.js
*.bundle.js

# Config that has its own style
tsconfig.json
eslint.config.mjs

# Environment (sensitive, no format)
.env
.env.*

# Root db only (migrations). src/db formatted with prettier-plugin-sql.
/db/

# Prisma (use: npm run db:prisma:format)
*.prisma
prisma/migrations
prisma/@prisma

# Vendored datasets (free-exercise-db, MIT) — keep upstream formatting
src/data/exercises.json
